# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-django-cosmos-postgres-aca
metadata:
    template: azure-django-cosmos-postgres-aca
infra:
    provider: "bicep"
services:
    web:
        project: demo_code
        module: web
        language: py
        host: containerapp

